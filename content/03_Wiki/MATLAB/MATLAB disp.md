`disp(X)` displays the value of variable `x` without printing the variable name

> [!NOTE] Syntax
> ```matlab
> disp(X)
> ```

> [!EXAMPLE]
> ```matlab
> A = [15 150];
> S = 'Hello World.';
> 
> disp(A)
> % 15  150
> 
> disp(S)
> % Hello World.
> ```
